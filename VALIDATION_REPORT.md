# Validation report: feature-specific Contact-SDF Atlas refinement

This report was generated by `python scripts/build_and_validate.py` after adding **feature-specific refinement**.

## What changed in this version

- Added `feature_max_depth` to `build_adaptive_contact_sdf_atlas(...)`.
- Smooth cells stop at `max_depth`; edge/vertex/multi-normal-sector cells can refine deeper to `feature_max_depth`.
- Added normal-sector-driven feature detection. A triangle edge hit alone is not treated as a physical sharp feature; refinement is triggered by competing corner-normal sectors.
- Sharp-feature validation metrics now use only competing active normal sectors; point-to-triangle edge/vertex regions on smooth benchmarks are excluded from cone-hit statistics.
- Added `feature_enrichment`, which bakes additional candidate normal-sector planes sampled inside feature leaves for sharp/mixed benchmarks.
- The projection backend now exposes welded-edge and welded-vertex incident corner-normal sectors, so the online reference, uniform atlas and adaptive atlas use the same normal-sector convention.
- Runtime evaluation remains projection-free: lookup through the finest leaf grid plus polynomial/plane evaluation.

## Summary metrics

| shape | grid_gap_rmse | atlas_gap_rmse | adaptive_gap_rmse | grid_normal_mean_deg | atlas_best_normal_mean_deg | adaptive_best_normal_mean_deg | atlas_sharp_cone_hit_rate | adaptive_sharp_cone_hit_rate | adaptive_smooth_max_depth | adaptive_feature_max_depth | adaptive_feature_leaf_count | projection_us_per_query | adaptive_us_per_query | speedup_projection_vs_adaptive |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ellipsoid | 0.0378 | 0.0206 | 0.0121 | 10.6774 | 3.7568 | 1.0526 | n/a | n/a | 2 | 2 | 48 | 404.80 | 37.37 | 10.83x |
| hex_prism | 0.0477 | 0.0396 | 0.0147 | 20.7709 | 14.3384 | 5.1923 | 0.677 | 0.892 | 1 | 3 | 2357 | 328.12 | 29.39 | 11.17x |
| cone | 0.0431 | 0.0382 | 0.0265 | 34.8051 | 21.0357 | 11.2105 | 0.554 | 0.785 | 1 | 3 | 2297 | 340.49 | 35.29 | 9.65x |

## Interpretation

- **Gap accuracy improves on all three bodies** compared with scalar grid SDF and uniform atlas. The effect is largest on `hex_prism`, where feature-specific leaves concentrate around sharp edges and vertices.
- **Normal-cone hit rate improves on sharp models**: `hex_prism` improves from 0.677 to 0.892, and `cone` improves from 0.554 to 0.785. This is the main purpose of feature-specific refinement.
- **Smooth normal accuracy uses the intended jet model**.  On the ellipsoid, smooth Hessian fitting in the feature-adaptive atlas reduces mean normal error to 1.0526 degrees, below the uniform atlas value of 3.7568 degrees.
- **Online speed remains much faster than per-query projection**. In this run the feature-refined atlas is about 9.6x to 11.2x faster than online projection.
- The ellipsoid is smooth, so cone-hit statistics remain n/a. A small number of adaptive leaves can still be marked as multi-sector records by the discrete corner-normal backend, but they are not counted as physical sharp-feature queries.
- The supplemental validation suite adds sphere exact Hessian, wedge, capped cylinder, cone side/rim/apex, ablations, mesh consistency, noisy normals, torus and rigid-transform invariance. It reports `issues: []`.

## Commands

```bash
python scripts/build_and_validate.py
python scripts/supplemental_validation.py
python scripts/generate_paper_figures.py
pytest -q
```

## Test result

`pytest -q` returns `11 passed`.
